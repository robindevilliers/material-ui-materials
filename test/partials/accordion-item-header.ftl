<span class="accordion-header ${classes} ${active?string('','collapsed')}" id="heading-${id!}" data-toggle="collapse"
    data-target="#panel-${id!}" aria-expanded="false" aria-controls="flush-collapseOne" style="${textualStyles}">
    ${content}
</span>